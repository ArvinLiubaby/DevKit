### 🎯 1. 项目定位与愿景

*   **项目名称**：可以暂定为 `DevKit`，你也可以发挥创意想一个独特的名字。
*   **核心定位**：一款**本地优先、离线可用、跨平台**的开发者效率工具集。
*   **设计哲学**：
    *   **隐私至上**：所有数据处理均在本地完成，**绝不将你的代码或数据上传至任何服务器**。
    *   **极致轻量**：告别臃肿的Electron应用，拥抱启动迅速、内存占用低的Tauri。
    *   **高效专注**：通过全局搜索或快捷键，快速定位并使用工具，减少在多个网页或应用间的切换。
    *   **赏心悦目**：提供现代化、可定制的UI，让编码工作也能成为一种视觉享受。

### 🛠️ 2. 技术栈推荐

*   **前端框架**：**Vue 3** (Composition API) 或 **React**，配合 **TypeScript** 提供类型安全。
*   **桌面壳**：**Tauri 2**。这是实现“轻量、跨平台”的关键。
*   **构建工具**：**Vite**，提供极快的开发体验。
*   **UI组件库**：可选择 **Naive UI**、**Ant Design** 或 **Tailwind CSS** 配合 **Radix UI**，快速构建美观界面。
*   **代码编辑器**：**Monaco Editor**，为你的工具箱提供VS Code级别的编辑体验。
*   **核心依赖**：按需引入，如 `qrcode`、`js-yaml`、`crypto-js` 等。
*   **Tauri 官方插件**（按需启用）：
    *   `tauri-plugin-updater`：自动更新机制。
    *   `tauri-plugin-global-shortcut`：全局快捷键唤起。
    *   `tauri-plugin-single-instance`：单实例锁，避免多开抢占快捷键。
    *   `tauri-plugin-store`：用户设置持久化。
    *   `tauri-plugin-clipboard-manager`：剪贴板读写。
    *   `tauri-plugin-shell`：按需调用系统命令。

### 📦 3. 功能模块规划（MVP & 后续）

开发时可以分阶段进行，先完成核心功能，再逐步扩展。以下是一个参考列表：

#### 🚀 Phase 1: 核心工具集 (MVP)

*   **编解码**：Base64, URL, HTML 编码/解码。
*   **格式化/美化**：JSON, XML, YAML, SQL 的格式化和压缩。
*   **哈希与加密**：MD5, SHA-1, SHA-256 等哈希计算；AES, DES 加解密。
*   **时间戳转换**：Unix时间戳与可读日期时间的双向转换。
*   **生成器**：UUID, 密码, 二维码生成。
*   **文本对比**：Diff Checker。

#### ⚡ Phase 2: 进阶功能

*   **正则表达式测试器**：支持实时匹配与结果高亮。
*   **JWT解码器**：解析并高亮显示JWT的头部、负载和签名。
*   **颜色工具**：颜色选择器与格式转换 (HEX, RGB, HSL)。
*   **REST API客户端**：支持发送基本的HTTP请求并查看响应。
*   **图片压缩与转换**：支持常见图片格式的压缩与转换。

#### 🧰 Phase 3: 扩展与高级功能

*   **CRON表达式生成与解释**：可视化的Cron表达式工具。
*   **代码运行游乐场 (Playground)**：提供一个轻量级环境，快速测试HTML/CSS/JS代码。
*   **环境变量管理**：安全地管理和查看本地环境变量。
*   **Markdown编辑器**：支持实时预览的Markdown编辑工具。
*   **数据 Mock**：根据数据结构生成模拟数据 (如Faker.js)。

### 🖥️ 4. 平台与分发策略

*   **目标平台**：优先 **Windows 10/11 (x64)** 与 **Linux (x64)**；arm64 与 **macOS 后续再支持**。架构上避免平台耦合代码（抽象层 + 条件编译），为 macOS 预留扩展点，后续新增仅需补充 CI 矩阵与打包 target。
*   **Windows 分发**：**NSIS 安装包**（支持静默升级参数，配合自动更新），可选提供便携版（绿色版）。
*   **Linux 分发**：**deb 格式**（优先 Ubuntu/Debian 系），打包依赖：`libwebkit2gtk-4.1-0`、`libgtk-3-0`、`libayatana-appindicator3-1`（托盘）；需在 README 提供不同发行版的依赖安装说明。其他发行版（Fedora/Arch）提供 AppImage 或源码构建说明，后续可发布 APT 仓库。
*   **安装包体积目标**：< 15MB（不含可选组件），作为"极致轻量"的量化指标之一。

### 🔐 5. 安全与签名

*   **Windows 代码签名**：使用 OV 代码签名证书（EV 成本过高，暂不考虑），CI 中签名并加盖 RFC3161 时间戳；证书私钥存放于 CI Secrets（或 Azure Trusted Signing）。MVP 早期可暂缓，但需知晓未签名 exe 会触发 SmartScreen"未知发布者"警告。
*   **更新签名**：使用 `tauri signer` 生成更新密钥对；**私钥离线保管 + CI Secrets 双备份**，公钥内置 `tauri.conf.json`；所有更新包强制签名（updater 的硬性要求，不可省略）。
*   **数据安全基线**：Rust 侧输入校验、严格 CSP、Tauri capabilities 权限白名单（invoke 命令最小化授权）。

### 🔄 6. 更新机制设计

*   **方案**：`tauri-plugin-updater` + **GitHub Releases** 作为更新源（静态 `latest.json` 清单，官方兼容格式，后续可无缝切换自建服务器/CDN）。
*   **发布流程**（GitHub 托管）：
    *   代码仓库托管于 **GitHub 公开仓库**（Releases 资产可匿名下载，无需认证）。
    *   推送版本 tag（如 `v0.1.0`）→ GitHub Actions 矩阵构建（Windows NSIS / Linux deb）→ `tauri-action` 自动生成并附加 `latest.json` 清单与各平台安装包到 Release。
    *   应用内更新端点：`https://github.com/{owner}/{repo}/releases/latest/download/latest.json`。
    *   资产命名规范：`devkit_{version}_x64-setup.exe`（Windows）、`devkit_{version}_amd64.deb`（Linux），保证清单 URL 稳定引用。
*   **更新清单**：`version`、`pub_date`、`notes`（更新日志）、按平台分发的 `url` + `signature`（如 `windows-x86_64`、`linux-x86_64`）。
*   **检查策略**（与"离线优先"哲学协调）：
    *   启动后**后台延迟检查**，不阻塞启动，失败静默不打扰；
    *   设置页提供**手动检查更新**；
    *   发现新版本弹窗展示更新内容，支持"立即更新 / 稍后 / 忽略此版本"。
*   **平台差异**：
    *   **Windows**：自动下载 → 校验签名 → 拉起 NSIS 静默安装（`-updated` 参数）→ 重启应用；全程无需用户干预。
    *   **Linux (deb)**：⚠️ `dpkg` 安装需要 root 权限，无法静默自动升级。设计为**半自动**：应用内检测并下载新 `.deb` → 校验签名与校验和 → 引导用户通过系统包管理器安装（如 `sudo apt install ./devkit_x.x.x_amd64.deb`）。可选提供 **AppImage 变体**以支持全自动更新（updater 对 AppImage 免安装，可静默替换）。
*   **失败处理**：下载或校验失败 → 提示手动下载；安装失败 → 旧版本不受影响（覆盖安装可回滚）。
*   **隐私边界**：更新仅请求清单与安装包，**不上传任何数据**。

### ⚡ 7. 全局唤起与命令面板

*   **插件**：`tauri-plugin-global-shortcut`（全局快捷键）+ `tauri-plugin-single-instance`（单实例锁，防止多开抢占）。
*   **交互流程**：任意界面按下全局快捷键 → 弹出**无边框、置顶、居中的命令面板小窗** → 输入即搜（工具名 / 拼音 / 最近使用 / 收藏）→ `Enter` 打开主窗口并定位到目标工具，`Esc` 关闭面板。
*   **默认快捷键**：`Alt+Space`（可自定义；注册失败时自动降级并提示，注意与 Windows 系统菜单/输入法的键位冲突）。
*   **Linux Wayland 已知限制**：X11 下开箱即用；Wayland（尤其 GNOME）默认禁止第三方全局快捷键，需 `xdg-desktop-portal` 授权或安装 GNOME 扩展；KDE 支持较好。列为已知风险，README 中说明。
*   **托盘**：支持最小化到系统托盘（托盘插件），托盘菜单可快速唤起命令面板或退出。

### 🎨 8. UI/UX 设计方案

*   **布局**：采用经典的**侧边栏 + 主内容区**布局。侧边栏用于工具分类导航，主内容区展示当前工具。
*   **交互**：
    *   **全局搜索**：通过快捷键（如 `Cmd/Ctrl + Shift + P`）快速搜索并打开任何工具。
    *   **明暗主题**：内置对**深色模式**和**浅色模式**的支持。可以提供`Neon`、`OLED`等多种主题变体。
    *   **实时响应**：工具操作应“输入即所得”，提供即时的反馈。
*   **品牌**：设计一个简洁、现代的Logo和统一的视觉语言。

### 🏗️ 9. 架构设计建议

*   **模块化**：每个工具都是一个独立的、可插拔的模块。这有助于代码维护和未来的功能扩展。
*   **核心与UI分离**：将工具的核心**业务逻辑（纯函数）** 与**UI渲染**分离。这能提高代码的可测试性和复用性。
*   **状态管理**：对于复杂的应用状态，可以引入 **Pinia (Vue)** 或 **Zustand (React)** 进行管理。
*   **配置持久化**：使用Tauri的`tauri-plugin-store`或`localStorage`来保存用户设置，如主题、收藏的工具等。

### 🗺️ 10. 开发路线图

1.  **第1-2周：项目初始化**
    *   使用 Tauri + Vue/React 官方脚手架创建项目。
    *   配置好ESLint、Prettier等代码规范工具。
    *   设计好应用的基本布局（侧边栏 + 主区域）。
2.  **第3-4周：MVP核心功能开发**
    *   选择3-5个最核心的工具（如JSON格式化、时间戳转换、Base64编解码）进行开发。
    *   实现工具的路由和懒加载。
3.  **第5-6周：完善与打磨**
    *   实现应用内全局搜索（Cmd/Ctrl + Shift + P）。
    *   实现**全局唤起命令面板**（全局快捷键 + 单实例 + 置顶小窗）。
    *   实现明暗主题切换与用户配置持久化。
4.  **第7-8周：工程化建设**
    *   集成**自动更新机制**（updater + GitHub Releases），生成更新签名密钥。
    *   **Windows 代码签名**配置（CI 签名 + 时间戳）。
    *   配置 GitHub Actions **矩阵构建**（Windows NSIS / Linux deb 产物 + 更新清单）。
5.  **第9-10周：测试与发布**
    *   在 Windows 和 Linux 上全面测试（含升级流程、快捷键冲突、deb 依赖安装）。
    *   撰写项目 README（含各发行版依赖说明与 Wayland 限制），发布 v0.1.0。

### 📄 11. 开源与社区

*   **开源协议**：推荐使用 **MIT** 或 **GPL-3.0**。MIT更宽松，GPL则要求衍生项目也必须开源。
*   **社区建设**：在README中明确贡献指南，鼓励开发者提交新的工具模块或改进建议。

---

### 🚀 第一步：从MVP开始

为了避免一开始就陷入功能过多的困境，建议**严格遵循MVP（最小可行产品）理念**。可以先选定**5-10个最高频使用的工具**（例如：JSON格式化、时间戳转换、Base64编解码、UUID生成、哈希计算），把它们做到极致，再逐步添加新功能。参考`666tools`等项目，它们正是通过聚焦核心功能，快速推出了可用的产品。

一个清晰、模块化的设计方案是成功的关键。上面的规划中，你有哪个部分希望我展开得更详细一些吗？比如具体的功能实现逻辑，或是Tauri的开发细节？